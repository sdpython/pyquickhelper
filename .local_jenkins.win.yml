language: python
python:
  - {{Python34}}
  - {{Python35}}
  - {{Python27}}
  - {{Anaconda35}}
  - {{Anaconda27}}
  - {{WinPython35}}
virtualenv:
  - path: {{project_name}}
install:
  - pip install -r requirements.txt
script:
  - python setup.py unittests
  - python setup.py unittests [LONG]
  - python setup.py unittests [SKIP]
documentation:
  - if [ ${PYTHON} == "{{Python35}}" ] then
    python setup.py build_sphinx
    fi
